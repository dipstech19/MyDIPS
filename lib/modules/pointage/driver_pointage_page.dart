import 'package:flutter/material.dart';

class DriverPointagePage extends StatelessWidget {
  const DriverPointagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pointage Chauffeur')),
      body: const Center(child: Text('Page de Pointage Chauffeur')),
    );
  }
}
